// Compute the sum & product of an array of integers

numbers = [3, 9, 72, 83, 4, 17, 5];

// I thought the commented-out stuff could be useful, but I ended up opting for a simple solution. -pm
// spreadNumbers = [...numbers];

// function add(...rest) {
//     let sum = 0;
//     for(let i=0; i < rest.length; i++ ) {
//         sum += rest[i];
//     }
//     return sum;
// }

// add(numbers)

let[a,b,c,d,e,f,g]=numbers

console.log(a+b+c+d+e+f+g);
console.log(a*b*c*d*e*f*g);